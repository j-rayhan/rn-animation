
import { Dimensions } from 'react-native';
const { width, height } = Dimensions.get('window');
const ITEM_WIDTH = width * 0.6, SPACING = 16, ICON_SIZE = 56;

export { ITEM_WIDTH, SPACING, ICON_SIZE, height, width }